[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    integration: marks tests as integration tests (require database)
    evals: marks tests as LLM eval tests (slow, require Azure OpenAI + pgvector)
